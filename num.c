#include <stdio.h>

int main() {
    int num1, num2, hum , number;
    
    printf("Enter the first number: ");
    scanf("%d", &num1);
    
    printf("Enter the second number: ");
    scanf("%d", &num2);
    
    hum = num1 + num3;
    
    printf("The sum of %d and %d is %d.", num1, num2, hum);
    
    return 0;
}
